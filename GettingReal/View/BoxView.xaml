<Page x:Class="GettingReal.View.BoxView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:GettingReal.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="BoxView">
    <StackPanel Orientation="Vertical" Margin="10">

        <StackPanel Orientation="Horizontal" Margin="10">
        <!-- Left Side: ListView for Boxes -->
                <StackPanel Orientation="Vertical" Width="300" Margin="0,0,10,0" Background="LightBlue">
                    <TextBlock Text="Box List" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
                    <ListView ItemsSource="{Binding Boxes}" 
                          SelectedItem="{Binding SelectedBox}" 
                          Margin="0,0,0,10">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="150"/>
                                <GridViewColumn Header="Description" DisplayMemberBinding="{Binding Description}" Width="150"/>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </StackPanel>

            <!-- Right Side: Form for Add/Edit -->
            <StackPanel Orientation="Vertical" Width="300" Background="AntiqueWhite">
                <TextBlock Text="Manage Box" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

                <TextBlock Text="Name:" Margin="0,0,0,5"/>
                <TextBox Text="{Binding NewBoxName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                <TextBlock Text="Description:" Margin="0,0,0,5"/>
                <TextBox Text="{Binding NewBoxDescription, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                    <Button Content="Add" Command="{Binding AddBoxCommand}" Width="75" Margin="0,0,10,0"/>
                    <Button Content="Remove" Command="{Binding RemoveBoxCommand}" Width="75"/>
                </StackPanel>
            </StackPanel>
        </StackPanel>


        <!-- Materials Section -->
        <!--<StackPanel Orientation="Vertical" Width="400" Margin="10,0,0,0">
            <TextBlock Text="Materials in Box" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
            <ListView ItemsSource="{Binding MaterialsInBox}" Margin="0,0,0,10">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="150"/>
                        <GridViewColumn Header="Description" DisplayMemberBinding="{Binding Description}" Width="200"/>
                    </GridView>
                </ListView.View>
            </ListView>

            <TextBlock Text="Material Repository" FontSize="20" FontWeight="Bold" Margin="0,10,0,10"/>
            <ListView ItemsSource="{Binding MaterialRepository}" Margin="0,0,0,10">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="150"/>
                        <GridViewColumn Header="Description" DisplayMemberBinding="{Binding Description}" Width="200"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </StackPanel>
        -->
    </StackPanel>


</Page>
